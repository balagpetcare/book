"use client";
import { useState } from "react";
export function ReviewForm() { const [message, setMessage] = useState(""); async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); const f = new FormData(e.currentTarget); const r = await fetch("/api/reviews", { method: "POST", body: f }); const j = await r.json(); setMessage(r.ok ? "Review submitted for approval." : j.error); if (r.ok) e.currentTarget.reset(); } return <form className="review-form" onSubmit={submit}><h2>আপনার অভিজ্ঞতা শেয়ার করুন</h2><input name="orderNumber" placeholder="Delivered order number" required /><input name="mobile" placeholder="Mobile used in order" required inputMode="tel" /><input name="displayName" placeholder="Display name" required /><select name="rating" defaultValue="5"><option value="5">★★★★★</option><option value="4">★★★★</option><option value="3">★★★</option><option value="2">★★</option><option value="1">★</option></select><textarea name="comment" placeholder="Your review (optional)" rows={4} /><input name="photo" type="file" accept="image/jpeg,image/png,image/webp" /><button className="button button-primary">Submit review</button>{message && <p>{message}</p>}</form>; }
