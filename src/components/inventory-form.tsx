"use client";
import { useState } from "react";
export function InventoryForm() { const [message, setMessage] = useState(""); async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); const f = new FormData(e.currentTarget); const r = await fetch("/api/admin/inventory", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(Object.fromEntries(f.entries())) }); const j = await r.json(); setMessage(r.ok ? "Inventory ledger updated." : j.error); if (r.ok) window.location.reload(); } return <form className="inventory-form" onSubmit={submit}><select name="action" defaultValue="ADD"><option value="ADD">ADD stock</option><option value="REMOVE">REMOVE stock</option><option value="ADJUST">ADJUST stock</option></select><input type="number" name="quantity" min="0" placeholder="Quantity" required /><input name="reason" placeholder="Reason (required)" required /><button className="button button-primary">Record ledger entry</button>{message && <p>{message}</p>}</form>; }
